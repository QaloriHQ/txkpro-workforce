# GitHub bootstrap

The connected GitHub account did not contain a TXKPRO repository when this package was generated, and the available GitHub connector actions cannot create a new repository.

Create an empty repository such as `QaloriHQ/txkpro-workforce`, then copy this package into it and push `main`. Once it exists and is available to the connected GitHub app, the scaffold can be committed/updated through the GitHub connector as normal.
